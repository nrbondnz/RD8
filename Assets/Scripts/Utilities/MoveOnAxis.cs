using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Utilities
{
    /// <summary>
    /// TODO: Just and idea at the moment.  Apply a force to an object
    /// It would be either constant or triggered and expire either after a period of time or on an exit trigger
    ///
    /// strength of force
    /// type of force
    /// Color of force
    /// Sound of movement
    ///
    /// Idea was initially related to winning platform to make harder perhaps based on difficulty level
    /// Clearly a utility
    /// </summary>
    public class MoveOnAxis : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
